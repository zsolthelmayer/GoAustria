
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="header-container">


	<div class="img-container">
		<div class="inner-container">
			<h1>Munkavállals Ausztriában lépésről lépésre</h1>
			<h2>Ne várj! <br /> Tudj meg mindent az osztrák munkavállalásról! <br /> Ez a könyv az összes kérdésedet megválaszolja! </h2>

			<a class="btn" href="#" > ÉRDEKEL </a>
		</div>
	</div>
</div>

<div class="gray-block">
    <div class="row">
        <div class="col-sm-4">
            <img src="~/images/book2.png" style="height: 30%; padding: 40px;" />
        </div>
        <div class="col-sm-8"> 
            <h2>A könyvről</h2> 
            Szinte nincs olyan ember Magyarországon, akinek egy rokona, barátja, ismerőse ne döntött volna úgy, hogy tőlünk nyugatabbra próbálja meg felépíteni, vagy legalább megalapozni az életét.
            Látva a pozitív kimenetelű sztorikat, érthető módon nagy a kísértés, hogy erre az útra lépj. A cél pedig sokszor Ausztria, mert jók a körülmények, sok a lehetőség, és gyakran haza lehet látogatni a családhoz, barátookhoz. Legalábbis ez terjeng a köztudatban.
            Amikor azonban nekilátsz a megvalósításnak, rájössz, hogy azért ez nem annyira egyszerű.

        </div>
    </div>
</div>




<script src="http://ajax.aspnetcdn.com/ajax/jQuery/jquery-2.0.3.min.js"></script>

<script>


    

    /*
        var uri = 'api/links';

        $(document).ready(function () {
            $.getJSON(uri)
                .done(function (data) {
                    $.each(data, function (key, item) {
                        $('<li>', { text: formatItem(item) }).appendTo($('#links'));
                    });
                });
        });

        function formatItem(item) {
            return item.Sequence + ': ' + item.Title + ' url: ' + item.Url;
        }

        function find() {
            var id = $('#linkId').val();
            $.getJSON(uri + '/' + id)
                .done(function (data) {
                    $('#link').text(formatItem(data));
                })
                .fail(function (jqXHR, textStatus, err) {
                    $('#link').text('Error: ' + err);
                });

        }
        */
</script>

